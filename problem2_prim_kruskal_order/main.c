#include <stdio.h>

int main() {
    // Compare edge orders of Prim's and Kruskal's algorithms
    // Problem 2: Implement a function that takes a graph and two algorithms (Prim's and Kruskal's)
    // and compares the order in which edges are added to the minimum spanning tree (MST) by each algorithm.
    // The graph is represented as an adjacency list, and the function should return the order of edges added by each algorithm.
    // The function should also handle the case where the graph is not connected.
    // The function should also handle the case where the graph is a complete graph.
    // TODO: Implement this problem's solution
    return 0;
}
